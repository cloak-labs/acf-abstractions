/**
 * ACF instruction tooltips are now CSS-only (`css/acf-tooltip.css` via :has()).
 * This file is kept so existing enqueues/caches don't 404; it is a no-op.
 */
(function () {
  "use strict";
})();
